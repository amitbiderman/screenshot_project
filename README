



docker build -t amitbiderman/mimecast_project .

with docker run:

	$ docker run -v /app -v $(pwd):/app amitbiderman/mimecast_project google.com

with docker compose:
Change to desired URL in docker-compose.yml file

	$ docker-compose up
